node_modules/
bin/
ui/dist/
server/dist/
# server/src/ui-bundle.ts is generated by server/scripts/generate-ui-bundle.ts;
# a tiny stub is committed so tsc/IDEs can resolve the import — the generator
# overwrites it on each build with the real (content-hashed) embed manifest.
*.log
.DS_Store
bun.lock
bun.lockb
