---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: agentrequests.kapsis.aviadshiber.github.io
spec:
  group: kapsis.aviadshiber.github.io
  names:
    kind: AgentRequest
    listKind: AgentRequestList
    plural: agentrequests
    shortNames:
    - ar
    - agent
    singular: agentrequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.agent.type
      name: Agent
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.progress
      name: Progress
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AgentRequest is the Schema for the agentrequests API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgentRequestSpec defines the desired state of AgentRequest.
            properties:
              agent:
                description: Agent defines the agent configuration.
                properties:
                  command:
                    description: Command is the command to execute in the container.
                    items:
                      type: string
                    type: array
                  type:
                    description: Type is the agent type (claude-cli, codex-cli, aider,
                      gemini-cli).
                    type: string
                  workdir:
                    default: /workspace
                    description: Workdir is the working directory inside container.
                    type: string
                required:
                - type
                type: object
              audit:
                description: Audit configures audit logging.
                properties:
                  enabled:
                    description: Enabled controls whether audit logging is active.
                    type: boolean
                type: object
              environment:
                description: Environment defines environment configuration.
                properties:
                  configMounts:
                    description: ConfigMounts are ConfigMaps to mount as volumes.
                    items:
                      description: ConfigMount defines a ConfigMap to mount as a volume.
                      properties:
                        mountPath:
                          description: MountPath inside the container.
                          type: string
                        name:
                          description: Name of the ConfigMap.
                          type: string
                      required:
                      - mountPath
                      - name
                      type: object
                    type: array
                  secretRefs:
                    description: SecretRefs are references to Secrets to mount as
                      env vars.
                    items:
                      description: SecretRef references a Secret by name.
                      properties:
                        name:
                          description: Name of the Secret.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  vars:
                    description: Vars is a list of environment variables.
                    items:
                      description: EnvVar is a name-value environment variable pair.
                      properties:
                        name:
                          description: Name of the environment variable.
                          type: string
                        value:
                          description: Value of the environment variable.
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                type: object
              git:
                description: Git defines git repository configuration.
                properties:
                  baseBranch:
                    default: main
                    description: BaseBranch for new branches.
                    type: string
                  branch:
                    description: Branch to work on.
                    type: string
                  credentialSecretRef:
                    description: CredentialSecretRef is a reference to a Secret containing
                      git credentials.
                    properties:
                      key:
                        description: Key within the Secret.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  push:
                    default: false
                    description: Push indicates whether to push changes after completion.
                    type: boolean
                  repoUrl:
                    description: RepoUrl is the repository URL to clone.
                    type: string
                type: object
              image:
                description: Image is the container image to run.
                type: string
              liveness:
                description: Liveness configures agent liveness monitoring and auto-kill
                  for hung processes.
                properties:
                  checkIntervalSeconds:
                    default: 30
                    description: CheckIntervalSeconds is how often to check for agent
                      activity.
                    format: int32
                    minimum: 10
                    type: integer
                  enabled:
                    description: Enabled controls whether liveness monitoring is active.
                    type: boolean
                  gracePeriodSeconds:
                    default: 300
                    description: GracePeriodSeconds is how long to skip liveness checks
                      after agent start.
                    format: int32
                    type: integer
                  timeoutSeconds:
                    default: 1800
                    description: TimeoutSeconds is how long (in seconds) to wait with
                      no activity before killing the agent.
                    format: int32
                    minimum: 60
                    type: integer
                type: object
              network:
                description: Network defines network configuration.
                properties:
                  mode:
                    default: filtered
                    description: 'Mode is the network mode: none, filtered, open.'
                    enum:
                    - none
                    - filtered
                    - open
                    type: string
                type: object
              podAnnotations:
                additionalProperties:
                  type: string
                description: |-
                  PodAnnotations are arbitrary annotations to add to the created Pod.
                  Use this to integrate with annotation-based tools such as Vault/OpenBao
                  Agent Injector, Istio, Linkerd, or Prometheus scraping.
                type: object
              resources:
                description: Resources defines resource limits for the pod.
                properties:
                  cpu:
                    default: "4"
                    description: CPU limit (e.g., "4").
                    type: string
                  memory:
                    default: 8Gi
                    description: Memory limit (e.g., "8Gi").
                    type: string
                type: object
              security:
                description: Security defines security configuration.
                properties:
                  profile:
                    default: standard
                    description: 'Profile is the security profile: minimal, standard,
                      strict, paranoid.'
                    enum:
                    - minimal
                    - standard
                    - strict
                    - paranoid
                    type: string
                  serviceAccountName:
                    default: kapsis-agent
                    description: ServiceAccountName for the pod.
                    type: string
                type: object
              task:
                description: Task defines the task specification.
                properties:
                  inline:
                    description: Inline task description.
                    type: string
                  specConfigMapRef:
                    description: SpecConfigMapRef references a ConfigMap containing
                      the task spec.
                    properties:
                      key:
                        default: spec.md
                        description: Key within the ConfigMap.
                        type: string
                      name:
                        description: Name of the ConfigMap.
                        type: string
                    required:
                    - name
                    type: object
                type: object
              ttl:
                default: 3600
                description: TTL is the time-to-live in seconds (pod killed after
                  this).
                format: int32
                type: integer
            required:
            - agent
            - image
            type: object
          status:
            description: AgentRequestStatus defines the observed state of AgentRequest.
            properties:
              commitSha:
                description: CommitSha is the SHA of the commit created by the agent.
                type: string
              completedAt:
                description: CompletedAt is when the agent completed.
                format: date-time
                type: string
              conditions:
                description: Conditions represent the current state of the AgentRequest.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              error:
                description: Error message if failed.
                type: string
              exitCode:
                description: ExitCode is the container exit code.
                format: int32
                type: integer
              gist:
                description: Gist is a short summary of current work.
                type: string
              gistUpdatedAt:
                description: GistUpdatedAt is the timestamp of last gist update.
                format: date-time
                type: string
              message:
                description: Message is a human-readable status message.
                type: string
              phase:
                description: Phase is the current phase.
                type: string
              podName:
                description: PodName is the name of the created Pod.
                type: string
              prUrl:
                description: PrUrl is the URL of the PR created by the agent.
                type: string
              progress:
                description: Progress percentage (0-100).
                format: int32
                maximum: 100
                minimum: 0
                type: integer
              pushStatus:
                description: 'PushStatus indicates push result: success, failed, skipped.'
                type: string
              response:
                description: Response is the agent response summary.
                type: string
              startedAt:
                description: StartedAt is when the agent started.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
